@model IEnumerable<CaseMvp.Dtos.SkinDto>

@{
    ViewData["Title"] = "ShowAllSkins";
}

<h1>ShowAllSkins</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fload)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Quality)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fload)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quality)
            </td>
            <td>
                    <form asp-action="ShowAllSkins" method="post">
                        <input type="hidden" name="skinId" value="@item.Id" />
                        <button type="submit">dodaj</button>
                    </form>
            </td>
        </tr>
}
    </tbody>
</table>
<div>
    <a asp-action="IndexAdmin">Back to List</a>
</div>
